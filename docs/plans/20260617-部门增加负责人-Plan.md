# 部门增加负责人（多负责人版）实施计划

> **更新说明：** 第一版（单负责人）已实施完毕。本版重构为多负责人，将 `Department.LeaderId` 单字段改为 `DepartmentLeaders` 关联表。

**Goal:** 将部门负责人从单个改为多个，每个部门可设置多位负责人，所有负责人自动获得该部门（含子部门）成员参与项目的查看权限。

**Architecture:** 新增 `DepartmentLeader` 关联表（DepartmentId + UserId，1:N），取代 `Department.LeaderId` 单字段。API 改为接受/返回 `leaderIds`/`leaders` 数组。权限可见性逻辑改为查询关联表获取负责人 ID 列表。前端负责人选择从单选改为多选。

**Tech Stack:** ASP.NET Core 8 + EF Core + Vue 3 + TypeScript + Element Plus + Vite

## 全局约束

- 后端遵循现有风格：控制器直接操作 `_db`
- 数据库使用 `EnsureCreated()` + `Program.cs` 内联 SQL 做增量迁移
- 前端源文件在 `codes/frontend/src/`，构建输出到 `codes/backend/ProjectManagement.API/wwwroot/`
- 所有 API 响应格式：`{ code: 0, message: "success", data: ... }`
- 用户搜索 API：`GET /api/v1/users/search?keyword=`

---

### Task 1: 新增 DepartmentLeader 实体 + EF 配置，移除 Department 单 Leader 字段

**Files:**
- Create: `codes/backend/ProjectManagement.Domain/Entities/DepartmentLeader.cs`
- Modify: `codes/backend/ProjectManagement.Domain/Entities/Department.cs`
- Modify: `codes/backend/ProjectManagement.Infrastructure/Data/AppDbContext.cs`（DbSet + OnModelCreating）

**Interfaces:**
- Produces: `DepartmentLeader` 实体（Id, DepartmentId, UserId, Department, User）
- Produces: `Department.Leaders` 导航属性（替代原 `LeaderId`/`Leader`）
- Produces: `DbSet<DepartmentLeader>`

- [ ] **Step 1: 创建 DepartmentLeader 实体**

新建 `codes/backend/ProjectManagement.Domain/Entities/DepartmentLeader.cs`：

```csharp
namespace ProjectManagement.Domain.Entities;

public class DepartmentLeader
{
    public long Id { get; set; }
    public long DepartmentId { get; set; }
    public long UserId { get; set; }
    public Department Department { get; set; } = null!;
    public User User { get; set; } = null!;
}
```

- [ ] **Step 2: 修改 Department 实体，移除单 Leader 字段**

替换 `codes/backend/ProjectManagement.Domain/Entities/Department.cs`：

```csharp
namespace ProjectManagement.Domain.Entities;

public class Department
{
    public long Id { get; set; }
    public string Name { get; set; } = "";
    public long? ParentId { get; set; }
    public int SortOrder { get; set; }
    public ICollection<User> Users { get; set; } = new List<User>();
    public ICollection<DepartmentLeader> Leaders { get; set; } = new List<DepartmentLeader>();
}
```

- [ ] **Step 3: 在 AppDbContext 中注册 DbSet**

在 `codes/backend/ProjectManagement.Infrastructure/Data/AppDbContext.cs` 的 `DbSet<Department>` 行之后添加：

```csharp
public DbSet<DepartmentLeader> DepartmentLeaders => Set<DepartmentLeader>();
```

- [ ] **Step 4: 修改 OnModelCreating 中的 Department 配置**

替换之前在 `AppDbContext.cs` 中添加的 `modelBuilder.Entity<Department>(...)` 配置块（单 Leader 配置）：

```csharp
modelBuilder.Entity<Department>(e =>
{
    e.HasMany(d => d.Leaders)
        .WithOne(l => l.Department)
        .HasForeignKey(l => l.DepartmentId)
        .OnDelete(DeleteBehavior.Cascade);
    e.HasIndex(d => d.ParentId);
});

modelBuilder.Entity<DepartmentLeader>(e =>
{
    e.ToTable("DepartmentLeaders");
    e.HasKey(x => x.Id);
    e.HasIndex(x => x.DepartmentId);
    e.HasIndex(x => x.UserId);
    e.HasIndex(x => new { x.DepartmentId, x.UserId }).IsUnique();
    e.HasOne(x => x.User)
        .WithMany()
        .HasForeignKey(x => x.UserId)
        .OnDelete(DeleteBehavior.Restrict);
});
```

- [ ] **Step 5: 编译验证**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/backend"
dotnet build ProjectManagement.Domain/ProjectManagement.Domain.csproj
dotnet build ProjectManagement.Infrastructure/ProjectManagement.Infrastructure.csproj
```

Expected: Build succeeded.

- [ ] **Step 6: Commit**

```bash
git add codes/backend/ProjectManagement.Domain/Entities/Department.cs codes/backend/ProjectManagement.Domain/Entities/DepartmentLeader.cs codes/backend/ProjectManagement.Infrastructure/Data/AppDbContext.cs
git commit -m "refactor: replace Department single LeaderId with DepartmentLeaders junction table"
```

---

### Task 2: 数据库迁移 SQL（建 DepartmentLeaders 表 + 旧数据迁移）

**Files:**
- Modify: `codes/backend/ProjectManagement.API/Program.cs`

**Interfaces:**
- Consumes: `DepartmentLeaders` 表（Task 1）
- Produces: 数据库建表 + 旧数据迁移 + 移除旧列

- [ ] **Step 1: 在 Program.cs 中添加迁移 SQL**

找到 `AddColumnIfNotExists(db, "Departments", "LeaderId", ...)`（上次添加的），替换为：

```csharp
// 创建 DepartmentLeaders 表（多负责人）
db.Database.ExecuteSqlRaw(@"IF NOT EXISTS (SELECT 1 FROM sys.objects WHERE object_id = OBJECT_ID(N'[dbo].[DepartmentLeaders]') AND type = 'U')
CREATE TABLE [DepartmentLeaders] ([Id] BIGINT IDENTITY(1,1) NOT NULL, [DepartmentId] BIGINT NOT NULL, [UserId] BIGINT NOT NULL, CONSTRAINT [PK_DepartmentLeaders] PRIMARY KEY CLUSTERED ([Id]), FOREIGN KEY ([DepartmentId]) REFERENCES [Departments]([Id]) ON DELETE CASCADE, FOREIGN KEY ([UserId]) REFERENCES [Users]([Id]))");
db.Database.ExecuteSqlRaw(@"IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_DepartmentLeaders_DepartmentId' AND object_id = OBJECT_ID(N'[dbo].[DepartmentLeaders]'))
CREATE INDEX [IX_DepartmentLeaders_DepartmentId] ON [DepartmentLeaders] ([DepartmentId])");
db.Database.ExecuteSqlRaw(@"IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_DepartmentLeaders_UserId' AND object_id = OBJECT_ID(N'[dbo].[DepartmentLeaders]'))
CREATE INDEX [IX_DepartmentLeaders_UserId] ON [DepartmentLeaders] ([UserId])");
db.Database.ExecuteSqlRaw(@"IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE name = 'IX_DepartmentLeaders_DeptId_UserId' AND object_id = OBJECT_ID(N'[dbo].[DepartmentLeaders]'))
CREATE UNIQUE INDEX [IX_DepartmentLeaders_DeptId_UserId] ON [DepartmentLeaders] ([DepartmentId], [UserId])");

// 迁移旧数据：将 Departments.LeaderId 迁移到 DepartmentLeaders
var conn = db.Database.GetDbConnection();
var wasOpen = conn.State == System.Data.ConnectionState.Open;
if (!wasOpen) conn.Open();
using var cmd = conn.CreateCommand();
cmd.CommandText = "SELECT COUNT(*) FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME = 'Departments' AND COLUMN_NAME = 'LeaderId'";
var oldLeaderIdExists = (int)cmd.ExecuteScalar()! > 0;
if (!wasOpen) conn.Close();
if (oldLeaderIdExists)
{
    db.Database.ExecuteSqlRaw(@"INSERT INTO [DepartmentLeaders] ([DepartmentId], [UserId])
SELECT [Id], [LeaderId] FROM [Departments] WHERE [LeaderId] IS NOT NULL
AND NOT EXISTS (SELECT 1 FROM [DepartmentLeaders] WHERE [DepartmentLeaders].[DepartmentId] = [Departments].[Id] AND [DepartmentLeaders].[UserId] = [Departments].[LeaderId])");
}
```

- [ ] **Step 2: 编译验证**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/backend"
dotnet build ProjectManagement.API/ProjectManagement.API.csproj
```

Expected: Build succeeded.

- [ ] **Step 3: Commit**

```bash
git add codes/backend/ProjectManagement.API/Program.cs
git commit -m "feat: add DepartmentLeaders table migration and data migration from LeaderId"
```

---

### Task 3: 更新后端 Department API（改为 leaders 数组）

**Files:**
- Modify: `codes/backend/ProjectManagement.API/Controllers/SystemController.cs`（GetDepartments, CreateDepartment, UpdateDepartment, SaveDepartmentRequest）
- Modify: `codes/backend/ProjectManagement.API/Controllers/CommonController.cs`（GetDepartments）

**Interfaces:**
- Consumes: `Department.Leaders` 导航属性（Task 1）
- Produces: `leaders` 数组（`[{id, realName}]`），接受 `leaderIds: number[]`

- [ ] **Step 1: 修改 SaveDepartmentRequest**

```csharp
public class SaveDepartmentRequest
{
    public string Name { get; set; } = "";
    public long? ParentId { get; set; }
    public int SortOrder { get; set; }
    public List<long> LeaderIds { get; set; } = new();
}
```

- [ ] **Step 2: 修改 SystemController.GetDepartments**

```csharp
[HttpGet("departments")]
public async Task<IActionResult> GetDepartments()
{
    var depts = await _db.Departments
        .OrderBy(d => d.SortOrder)
        .Select(d => new {
            d.Id, d.Name, d.ParentId, d.SortOrder,
            Leaders = d.Leaders.OrderBy(l => l.Id).Select(l => new { l.UserId, l.User.RealName }).ToList()
        })
        .ToListAsync();
    return Ok(new { code = 0, message = "success", data = depts });
}
```

- [ ] **Step 3: 修改 SystemController.CreateDepartment**

```csharp
[HttpPost("departments")]
public async Task<IActionResult> CreateDepartment([FromBody] SaveDepartmentRequest request)
{
    var dept = new ProjectManagement.Domain.Entities.Department
    {
        Name = request.Name.Trim(),
        ParentId = request.ParentId,
        SortOrder = request.SortOrder
    };
    _db.Departments.Add(dept);
    await _db.SaveChangesAsync();

    // 设置负责人
    if (request.LeaderIds.Count > 0)
    {
        foreach (var userId in request.LeaderIds)
        {
            _db.DepartmentLeaders.Add(new ProjectManagement.Domain.Entities.DepartmentLeader
            {
                DepartmentId = dept.Id,
                UserId = userId
            });
        }
        await _db.SaveChangesAsync();
    }

    return Ok(new { code = 0, message = "success", data = new { id = dept.Id } });
}
```

- [ ] **Step 4: 修改 SystemController.UpdateDepartment**

```csharp
[HttpPut("departments/{id:long}")]
public async Task<IActionResult> UpdateDepartment(long id, [FromBody] SaveDepartmentRequest request)
{
    var dept = await _db.Departments.FindAsync(id);
    if (dept == null) return Ok(new { code = 404, message = "部门不存在" });

    if (request.ParentId.HasValue && request.ParentId.Value == id)
        throw new InvalidOperationException("不能将自身设为上级部门");

    dept.Name = request.Name.Trim();
    dept.ParentId = request.ParentId;
    dept.SortOrder = request.SortOrder;

    // 更新负责人：先删后加
    var existing = _db.DepartmentLeaders.Where(l => l.DepartmentId == id);
    _db.DepartmentLeaders.RemoveRange(existing);
    if (request.LeaderIds.Count > 0)
    {
        foreach (var userId in request.LeaderIds)
        {
            _db.DepartmentLeaders.Add(new ProjectManagement.Domain.Entities.DepartmentLeader
            {
                DepartmentId = id,
                UserId = userId
            });
        }
    }

    await _db.SaveChangesAsync();
    return Ok(new { code = 0, message = "success" });
}
```

- [ ] **Step 5: 修改 CommonController.GetDepartments**

```csharp
[HttpGet("departments")]
public async Task<IActionResult> GetDepartments()
{
    var depts = await _db.Departments
        .OrderBy(d => d.SortOrder)
        .Select(d => new {
            d.Id, d.Name, d.ParentId,
            Leaders = d.Leaders.OrderBy(l => l.Id).Select(l => new { l.UserId, l.User.RealName }).ToList()
        })
        .ToListAsync();
    return Ok(new { code = 0, message = "success", data = depts });
}
```

- [ ] **Step 6: 编译验证**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/backend"
dotnet build ProjectManagement.API/ProjectManagement.API.csproj
```

Expected: Build succeeded.

- [ ] **Step 7: Commit**

```bash
git add codes/backend/ProjectManagement.API/Controllers/SystemController.cs codes/backend/ProjectManagement.API/Controllers/CommonController.cs
git commit -m "refactor: Department API uses leaders array instead of single leaderId"
```

---

### Task 4: 更新权限逻辑改为多 LeaderId 查询

**Files:**
- Modify: `codes/backend/ProjectManagement.API/Auth/ProjectDataScopeResolver.cs`（GetLeaderDeptTreeIdsAsync）
- Modify: `codes/backend/ProjectManagement.API/Auth/ProjectPermissionHelper.cs`（CanAccessProjectAsync，LeaderDeptIds 收集）

**Interfaces:**
- Consumes: `DepartmentLeaders` 表（Task 1）
- Produces: 权限查询基于多负责人 ID 列表

- [ ] **Step 1: 修改 GetLeaderDeptTreeIdsAsync 查询 DepartmentLeaders 表**

替换 `codes/backend/ProjectManagement.API/Auth/ProjectDataScopeResolver.cs` 中的 `GetLeaderDeptTreeIdsAsync` 方法：

```csharp
/// <summary>获取用户担任负责人的部门及其所有子孙部门 ID</summary>
private static async Task<List<long>> GetLeaderDeptTreeIdsAsync(AppDbContext db, long userId)
{
    var leaderDeptIds = await db.DepartmentLeaders
        .Where(l => l.UserId == userId)
        .Select(l => l.DepartmentId)
        .Distinct()
        .ToListAsync();
    if (leaderDeptIds.Count == 0) return new List<long>();

    var allDepts = await db.Departments.AsNoTracking()
        .Select(d => new { d.Id, d.ParentId }).ToListAsync();
    var result = new HashSet<long>(leaderDeptIds);
    void Collect(long parentId)
    {
        foreach (var d in allDepts.Where(d => d.ParentId == parentId))
        {
            if (result.Add(d.Id)) Collect(d.Id);
        }
    }
    foreach (var rootId in leaderDeptIds) Collect(rootId);
    return result.ToList();
}
```

- [ ] **Step 2: 修改 CanAccessProjectAsync 中的部门负责人检查逻辑**

替换 `codes/backend/ProjectManagement.API/Auth/ProjectPermissionHelper.cs` 中 `CanAccessProjectAsync` 方法里收集 LeaderDeptIds 的代码块：

将：
```csharp
var leaderDeptIds = await db.Departments
    .Where(d => d.LeaderId == userId)
    .Select(d => d.Id)
    .ToListAsync();
```

改为：
```csharp
var leaderDeptIds = await db.DepartmentLeaders
    .Where(l => l.UserId == userId)
    .Select(l => l.DepartmentId)
    .Distinct()
    .ToListAsync();
```

其余递归收集子部门的逻辑保持不变。

- [ ] **Step 3: 编译验证**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/backend"
dotnet build ProjectManagement.API/ProjectManagement.API.csproj
```

Expected: Build succeeded.

- [ ] **Step 4: Commit**

```bash
git add codes/backend/ProjectManagement.API/Auth/ProjectDataScopeResolver.cs codes/backend/ProjectManagement.API/Auth/ProjectPermissionHelper.cs
git commit -m "refactor: query DepartmentLeaders table for multi-leader visibility"
```

---

### Task 5: 更新前端类型定义 + API 层

**Files:**
- Modify: `codes/frontend/src/types/system.ts`（DepartmentItem）
- Modify: `codes/frontend/src/api/system.ts`（createDepartment, updateDepartment 签名）

- [ ] **Step 1: 更新 DepartmentItem 类型**

```typescript
export interface DepartmentItem {
  id: number
  name: string
  parentId: number | null
  sortOrder: number
  leaders: { userId: number; realName: string }[]
}
```

- [ ] **Step 2: 更新 API 函数签名**

```typescript
export function createDepartment(data: { name: string; parentId?: number | null; sortOrder: number; leaderIds?: number[] }) {
  return request.post<ApiResponse<{ id: number }>>('/system/departments', data)
}

export function updateDepartment(id: number, data: { name: string; parentId?: number | null; sortOrder: number; leaderIds?: number[] }) {
  return request.put<ApiResponse<null>>(`/system/departments/${id}`, data)
}
```

- [ ] **Step 3: 编译验证前端**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/frontend"
npx vue-tsc --noEmit
```

Expected: 无类型错误。

- [ ] **Step 4: Commit**

```bash
git add codes/frontend/src/types/system.ts codes/frontend/src/api/system.ts
git commit -m "refactor: update frontend types and API for multi-leader support"
```

---

### Task 6: 更新 UserManagement.vue 负责人 UI 为多选 + DepartmentManagement.vue 编辑框

**Files:**
- Modify: `codes/frontend/src/views/system/UserManagement.vue`
- Modify: `codes/frontend/src/views/system/DepartmentManagement.vue`

- [ ] **Step 1: 更新 UserManagement.vue 中的 currentDeptInfo 显示**

工具栏中负责人显示改为多个：

```html
<template v-if="currentDeptInfo?.leaders?.length">
  <span style="margin-left: 12px; color: #909399;">|</span>
  <span style="margin-left: 12px;">负责人：</span>
  <el-tag
    v-for="l in currentDeptInfo!.leaders"
    :key="l.userId"
    type="warning"
    size="small"
    style="margin-right: 4px;"
  >{{ l.realName }}</el-tag>
</template>
```

- [ ] **Step 2: 更新 UserManagement.vue 中 leader 对话框为多选模式**

将 `leaderSelectedId`（单值）改为 `leaderSelectedIds: number[]`：

```typescript
const leaderSelectedIds = ref<number[]>([])

function handleSetLeader() {
  if (!selectedDeptId.value) return
  const dept = currentDeptInfo.value
  leaderSelectedIds.value = dept?.leaders?.map(l => l.userId) ?? []
  leaderSearchKeyword.value = ''
  leaderSearchResults.value = []
  leaderDialogVisible.value = true
}

function handleLeaderToggle(user: { id: number }) {
  const idx = leaderSelectedIds.value.indexOf(user.id)
  if (idx >= 0) leaderSelectedIds.value.splice(idx, 1)
  else leaderSelectedIds.value.push(user.id)
}

function handleLeaderClearAll() {
  leaderSelectedIds.value = []
}

async function handleLeaderConfirm() {
  if (!selectedDeptId.value) return
  const dept = currentDeptInfo.value
  if (!dept) return
  try {
    await updateDepartment(selectedDeptId.value, {
      name: dept.name,
      parentId: dept.parentId,
      sortOrder: dept.sortOrder,
      leaderIds: leaderSelectedIds.value
    })
    ElMessage.success('负责人设置成功')
    leaderDialogVisible.value = false
    await getDepartmentList().then(res => { deptOptions.value = res.data })
  } catch { /* 错误由拦截器处理 */ }
}
```

- [ ] **Step 3: 更新 UserManagement.vue 对话框模板为多选表格**

```html
<!-- 设置部门负责人对话框 -->
<el-dialog
  v-model="leaderDialogVisible"
  title="设置部门负责人（多选）"
  width="500px"
  :close-on-click-modal="false"
>
  <div style="margin-bottom: 12px; color: #606266;">
    当前部门：<strong>{{ currentDeptInfo?.name }}</strong>
    <span v-if="currentDeptInfo?.leaders?.length" style="margin-left: 12px;">
      当前负责人：
      <el-tag v-for="l in currentDeptInfo!.leaders" :key="l.userId" type="warning" size="small" style="margin-right: 4px;">{{ l.realName }}</el-tag>
    </span>
  </div>

  <el-input
    v-model="leaderSearchKeyword"
    placeholder="搜索用户（姓名 / 用户名）"
    clearable
    @input="handleLeaderSearch"
    style="margin-bottom: 12px;"
  />

  <div v-if="leaderSelectedIds.length > 0" style="margin-bottom: 12px; padding: 8px 12px; background: #f0f9eb; border-radius: 4px; display: flex; align-items: center; justify-content: space-between;">
    <span>已选择 <strong>{{ leaderSelectedIds.length }}</strong> 人</span>
    <el-button size="small" type="danger" plain @click="handleLeaderClearAll">全部清除</el-button>
  </div>

  <el-table
    :data="leaderSearchResults"
    v-loading="leaderDialogLoading"
    border
    size="small"
    style="width: 100%"
    max-height="300"
    @selection-change="(rows: any[]) => { rows.forEach(r => { if (!leaderSelectedIds.includes(r.id)) leaderSelectedIds.push(r.id) }) }"
  >
    <el-table-column type="selection" width="45" />
    <el-table-column prop="realName" label="姓名" width="120" />
    <el-table-column prop="username" label="用户名" width="130" />
    <el-table-column label="所属部门" min-width="150" show-overflow-tooltip>
      <template #default="{ row }">{{ row.departmentName || '—' }}</template>
    </el-table-column>
  </el-table>

  <template #footer>
    <el-button @click="leaderDialogVisible = false">取消</el-button>
    <el-button type="danger" @click="handleLeaderConfirm">确定</el-button>
  </template>
</el-dialog>
```

Wait — using `@selection-change` with manual `leaderSelectedIds` tracking is fragile. Let me use `el-table` with `ref` + `toggleRowSelection` pattern instead. But that requires a `ref` on the table and matching by row key. Let me simplify:

Use a checkbox column manually:

Actually, the simplest approach is to use `el-select` with `multiple` and `filterable` for the user search + multi-select, replacing the entire table-based UI. This is more idiomatic for multi-select:

```html
<el-dialog v-model="leaderDialogVisible" title="设置部门负责人（多选）" width="500px">
  <div style="margin-bottom: 12px;">
    当前部门：<strong>{{ currentDeptInfo?.name }}</strong>
  </div>
  <el-select
    v-model="leaderSelectedIds"
    multiple
    filterable
    remote
    reserve-keyword
    placeholder="搜索用户（姓名 / 用户名）"
    :remote-method="handleLeaderSearch"
    :loading="leaderDialogLoading"
    style="width: 100%"
    value-key="id"
  >
    <el-option
      v-for="u in leaderSearchResults"
      :key="u.id"
      :label="`${u.realName} (${u.username})`"
      :value="u.id"
    />
  </el-select>
  <template #footer>
    <el-button @click="leaderDialogVisible = false">取消</el-button>
    <el-button type="danger" @click="handleLeaderConfirm">确定</el-button>
  </template>
</el-dialog>
```

This is much cleaner. `leaderSelectedIds` is `ref<number[]>([])`, `el-select` with `multiple` handles the multi-select, `filterable` + `remote` handles search. `value-key="id"` ensures the value comparison works.

- [ ] **Step 4: 更新 DepartmentManagement.vue 编辑表单（增加负责人多选）**

`DepartmentManagement.vue` 的 `form` 需要增加 `leaderIds`，`handleEdit` 需要回填。但这会涉及更大的改动。按 spec 需求点，"在人员管理"页面设置负责人，所以 DepartmentManagement 可以暂不改动，负责人设置主要在 UserManagement 的工具栏中操作。

- [ ] **Step 5: 编译验证前端**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/frontend"
npx vue-tsc --noEmit
```

Expected: 无类型错误。

- [ ] **Step 6: Commit**

```bash
git add codes/frontend/src/views/system/UserManagement.vue
git commit -m "refactor: multi-select leader UI in user management"
```

---

### Task 7: 构建前端并部署到 wwwroot

- [ ] **Step 1: 构建前端**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/frontend"
npm run build
```

- [ ] **Step 2: Commit**

```bash
git add codes/backend/ProjectManagement.API/wwwroot/
git commit -m "chore: rebuild frontend with multi-leader support"
```

---

## 验证清单

1. **设置多个负责人**：人员管理 → 选中部门 → 点击「设置负责人」→ 搜索并选择 2-3 人 → 确定 → 工具栏显示多个负责人 Tag
2. **清除负责人**：重新打开对话框 → 全部清除 → 确定 → 负责人信息消失
3. **权限**：部门负责人 A、B 都能看到该部门（含子部门）成员参与的项目
4. **部门管理**：部门列表 API 返回 `leaders` 数组
