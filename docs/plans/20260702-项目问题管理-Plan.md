# Plan — 项目问题管理

> 日期：2026-07-02　|　状态：📋 待审批　|　关联 Spec：[20260702-项目问题管理.md](../specs/20260702-项目问题管理.md)

## 0. 目标

在现有项目体系中新增「问题管理」模块，涵盖：
- 项目内问题台账（CRUD + 三态流转 + 1:N 措施）
- 侧边栏全局问题视图（跨项目个人汇总 + 角标计数）
- 问题生命周期**完全独立**于项目状态
- 复用现有权限体系 + 新增 6 个独立权限码
- 附件复用现有文件体系

---

## 1. 改动清单

| # | 改动点 | 层 | 类型 | 说明 |
|---|--------|------|------|------|
| 1 | `ProjectIssue` 实体 | Domain | 新增 | 问题主表（20 字段） |
| 2 | `ProjectIssueMeasure` 实体 | Domain | 新增 | 措施子表（9 字段） |
| 3 | `ProjectFileItem` 补充字段 | Domain | 修改 | 新增 `SourceType`、`SourceId` 支持附件复用 |
| 4 | DbContext 配置 | Infrastructure | 新增 | DbSet + 关系 + 索引 |
| 5 | `DbInitializer` Seed | Infrastructure | 修改 | 字典 + 权限码 + 系统参数 |
| 6 | `IssuesController` | API | 新增 | 项目内 6 个 + 全局 3 个端点 |
| 7 | 项目删除级联 | API | 修改 | 删除项目时级联删除问题 + 措施 |
| 8 | TS 类型 + API 层 | Frontend | 新增 | `src/types/issue.ts` + `src/api/issue.ts` |
| 9 | `IssueDialog.vue` | Frontend | 新增 | 创建/编辑弹窗（三区域） |
| 10 | `IssueTab.vue` | Frontend | 新增 | 项目内问题 Tab 组件 |
| 11 | `IssueManagement.vue` | Frontend | 新增 | 全局问题管理页面 |
| 12 | `ProjectEditor.vue` | Frontend | 修改 | 注册「问题管理」Tab |
| 13 | 路由 + 菜单 + Store | Frontend | 修改 | 路由、菜单项、角标计数 |
| 14 | 前后端构建 + 联调 | — | 构建 | `npm run build` + `dotnet build` |

---

## 2. 全局约束

- 后端遵循现有分层：Domain（实体）→ Infrastructure（DbContext/Repository）→ API（Controller，业务逻辑直写）
- 数据库使用 `EnsureCreated()` + `DbInitializer` 的 Seed 机制做增量
- 前端源文件在 `codes/frontend/src/`，构建输出到 `codes/backend/ProjectManagement.API/wwwroot/`
- 构建命令：`cd codes/frontend && npm run build`
- 所有 API 响应格式：`{ code: 0, message: "success", data: ... }`
- 权限校验：`[RequirePermission("code")]` 特性 + Admin 自动豁免

---

## 3. 逐步实施

---

### Phase 1: 后端基础 — 实体 + DbContext + Seed

---

### Task 1: 新增 `ProjectIssue` 实体

**Files:**
- Create: `codes/backend/ProjectManagement.Domain/Entities/ProjectIssue.cs`

**Interfaces:**
- Produces: `ProjectIssue` 实体类（含所有字段 + 导航属性）

- [ ] **Step 1: 创建实体类**

```csharp
namespace ProjectManagement.Domain.Entities;

public class ProjectIssue
{
    public long Id { get; set; }

    // ── 归属 ──
    public long ProjectId { get; set; }
    public string IssueCode { get; set; } = "";
    public Project Project { get; set; } = null!;

    // ── 基本信息 ──
    public string Title { get; set; } = "";
    public string? Description { get; set; }
    public string IssueSource { get; set; } = "";
    public string IssueType { get; set; } = "";
    public string Severity { get; set; } = "一般";       // 重要 / 一般
    public string Priority { get; set; } = "一般";       // 紧急 / 一般
    public int Status { get; set; } = 0;                 // 0=待处理 1=处理中 2=已完成

    // ── 分析与日期 ──
    public string? CauseAnalysis { get; set; }
    public DateOnly DiscoveredDate { get; set; }
    public DateOnly? PlannedDate { get; set; }

    // ── 人员 ──
    public long? ResponsibleDeptId { get; set; }
    public string? ResponsibleDeptName { get; set; }
    public long AssigneeId { get; set; }
    public string? AssigneeName { get; set; }
    public long SubmitterId { get; set; }
    public string? SubmitterName { get; set; }
    public long CreatorId { get; set; }
    public string? CreatorName { get; set; }
    public long? VerifierId { get; set; }
    public string? VerifierName { get; set; }
    public DateOnly? VerifiedDate { get; set; }

    // ── 统计 ──
    public int ReopenCount { get; set; } = 0;

    // ── 审计 ──
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public DateTime? UpdatedAt { get; set; }

    // ── 导航 ──
    public List<ProjectIssueMeasure> Measures { get; set; } = new();
}
```

- [ ] **Step 2: 编译验证**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/backend"
dotnet build ProjectManagement.Domain/ProjectManagement.Domain.csproj
```

---

### Task 2: 新增 `ProjectIssueMeasure` 实体

**Files:**
- Create: `codes/backend/ProjectManagement.Domain/Entities/ProjectIssueMeasure.cs`

- [ ] **Step 1: 创建实体类**

```csharp
namespace ProjectManagement.Domain.Entities;

public class ProjectIssueMeasure
{
    public long Id { get; set; }
    public long IssueId { get; set; }
    public int SortOrder { get; set; }
    public string Measure { get; set; } = "";
    public string? MeasureType { get; set; }
    public long? ResponsibleDeptId { get; set; }
    public string? ResponsibleDeptName { get; set; }
    public long? ResponsibleUserId { get; set; }
    public string? ResponsibleUserName { get; set; }
    public string? Remark { get; set; }
    public DateOnly? PlannedDate { get; set; }

    public ProjectIssue Issue { get; set; } = null!;
}
```

- [ ] **Step 2: 编译验证**

```bash
dotnet build ProjectManagement.Domain/ProjectManagement.Domain.csproj
```

---

### Task 3: DbContext 注册 + 关系配置

**Files:**
- Modify: `codes/backend/ProjectManagement.Infrastructure/Data/AppDbContext.cs`

- [ ] **Step 1: 添加 DbSet**

在 `AppDbContext.cs` 的 DbSet 声明区域（约 40 行，`ProjectFileVersionFiles` 后面）：

```csharp
public DbSet<ProjectIssue> ProjectIssues => Set<ProjectIssue>();
public DbSet<ProjectIssueMeasure> ProjectIssueMeasures => Set<ProjectIssueMeasure>();
```

- [ ] **Step 2: 在 `OnModelCreating` 中添加表和关系配置**

在 `OnModelCreating` 末尾（`base.OnModelCreating(modelBuilder)` 之前）：

```csharp
modelBuilder.Entity<ProjectIssue>(e =>
{
    e.ToTable("ProjectIssues");
    e.HasKey(x => x.Id);
    e.HasIndex(x => x.ProjectId);
    e.HasIndex(x => x.IssueCode).IsUnique();
    e.HasIndex(x => x.Status);
    e.HasIndex(x => x.AssigneeId);
    e.HasOne(x => x.Project)
        .WithMany()
        .HasForeignKey(x => x.ProjectId)
        .OnDelete(DeleteBehavior.Cascade);
    e.HasMany(x => x.Measures)
        .WithOne(m => m.Issue)
        .HasForeignKey(m => m.IssueId)
        .OnDelete(DeleteBehavior.Cascade);
});

modelBuilder.Entity<ProjectIssueMeasure>(e =>
{
    e.ToTable("ProjectIssueMeasures");
    e.HasKey(x => x.Id);
    e.HasIndex(x => x.IssueId);
});
```

- [ ] **Step 3: 编译验证**

```bash
dotnet build ProjectManagement.Infrastructure/ProjectManagement.Infrastructure.csproj
```

---

### Task 4: DbInitializer 种子数据

**Files:**
- Modify: `codes/backend/ProjectManagement.Infrastructure/Data/DbInitializer.cs`

- [ ] **Step 1: Seed 权限码**

在 `EnsureSeedAsync` 中，现有权限 Seed 之后添加：

```csharp
// ── 问题管理权限码 ──
if (!db.Permissions.Any(p => p.Code == "issue"))
{
    var projectPerm = await db.Permissions.FirstOrDefaultAsync(p => p.Code == "project");
    if (projectPerm != null)
    {
        var issueMenu = new Permission { Code = "issue", Name = "问题管理", Type = "Menu", ParentId = projectPerm.Id, SortOrder = 300 };
        db.Permissions.Add(issueMenu);
        await db.SaveChangesAsync();

        var children = new (string Code, string Name, string Type)[]
        {
            ("issue:list",   "查看问题列表", "Button"),
            ("issue:create", "创建问题",     "Button"),
            ("issue:edit",   "编辑问题",     "Button"),
            ("issue:delete", "删除问题",     "Button"),
            ("issue:assign", "指派责任人",   "Button"),
            ("issue:verify", "验证问题",     "Button"),
        };
        int sort = 100;
        foreach (var (code, name, type) in children)
        {
            if (!db.Permissions.Any(p => p.Code == code))
                db.Permissions.Add(new Permission { Code = code, Name = name, Type = type, ParentId = issueMenu.Id, SortOrder = sort });
            sort += 100;
        }
        await db.SaveChangesAsync();
    }
}

// ── 角色-权限分配 ──
var issuePerms = new[] { "issue:list", "issue:create", "issue:edit", "issue:delete", "issue:assign", "issue:verify" };
foreach (var roleCode in new[] { "admin", "project_admin" })
{
    var role = await db.Roles.FirstOrDefaultAsync(r => r.Code == roleCode);
    if (role != null)
    {
        foreach (var code in issuePerms)
        {
            var perm = await db.Permissions.FirstOrDefaultAsync(p => p.Code == code);
            if (perm != null && !db.RolePermissions.Any(rp => rp.RoleId == role.Id && rp.PermissionId == perm.Id))
                db.RolePermissions.Add(new RolePermission { RoleId = role.Id, PermissionId = perm.Id });
        }
    }
}
// PM: list, create, edit, assign, verify (无 delete)
var pmRole = await db.Roles.FirstOrDefaultAsync(r => r.Code == "project_manager");
if (pmRole != null)
{
    var pmPerms = new[] { "issue:list", "issue:create", "issue:edit", "issue:assign", "issue:verify" };
    foreach (var code in pmPerms)
    {
        var perm = await db.Permissions.FirstOrDefaultAsync(p => p.Code == code);
        if (perm != null && !db.RolePermissions.Any(rp => rp.RoleId == pmRole.Id && rp.PermissionId == perm.Id))
            db.RolePermissions.Add(new RolePermission { RoleId = pmRole.Id, PermissionId = perm.Id });
    }
}
// task_manager: list, create (无 edit/delete/assign/verify)
var tmRole = await db.Roles.FirstOrDefaultAsync(r => r.Code == "task_manager");
if (tmRole != null)
{
    foreach (var code in new[] { "issue:list", "issue:create" })
    {
        var perm = await db.Permissions.FirstOrDefaultAsync(p => p.Code == code);
        if (perm != null && !db.RolePermissions.Any(rp => rp.RoleId == tmRole.Id && rp.PermissionId == perm.Id))
            db.RolePermissions.Add(new RolePermission { RoleId = tmRole.Id, PermissionId = perm.Id });
    }
}
// user: issue:list only
var userRole = await db.Roles.FirstOrDefaultAsync(r => r.Code == "user");
if (userRole != null)
{
    var perm = await db.Permissions.FirstOrDefaultAsync(p => p.Code == "issue:list");
    if (perm != null && !db.RolePermissions.Any(rp => rp.RoleId == userRole.Id && rp.PermissionId == perm.Id))
        db.RolePermissions.Add(new RolePermission { RoleId = userRole.Id, PermissionId = perm.Id });
}
await db.SaveChangesAsync();
```

- [ ] **Step 2: Seed 字典条目**

```csharp
// ── 问题管理字典 ──
var issueDicts = new Dictionary<string, (string Name, string[] Items)>
{
    ["issue_source"] = ("问题来源", new[] { "内部审查", "客户反馈", "测试发现", "日常巡检", "其他" }),
    ["issue_type"]  = ("问题类型", new[] { "技术缺陷", "需求变更", "进度风险", "资源问题", "质量问题", "其他" }),
    ["measure_type"] = ("措施类型", new[] { "纠正措施", "预防措施", "临时措施", "其他" }),
};
foreach (var kv in issueDicts)
{
    if (!db.DictTypes.Any(d => d.DictCode == kv.Key))
        db.DictTypes.Add(new DictType { DictCode = kv.Key, DictName = kv.Value.Name, Status = 1, CreatedAt = DateTime.UtcNow });

    var dt = await db.DictTypes.FirstOrDefaultAsync(d => d.DictCode == kv.Key);
    if (dt != null)
    {
        for (int i = 0; i < kv.Value.Items.Length; i++)
        {
            if (!db.DictItems.Any(di => di.DictType == kv.Key && di.DictCode == kv.Value.Items[i]))
                db.DictItems.Add(new DictItem { DictType = kv.Key, DictCode = kv.Value.Items[i], DictLabel = kv.Value.Items[i], SortOrder = (i + 1) * 10, Status = 1 });
        }
    }
}
await db.SaveChangesAsync();
```

- [ ] **Step 3: Seed 系统参数**

```csharp
if (!db.SysParams.Any(p => p.ParamKey == "issue_code_prefix"))
    db.SysParams.Add(new SysParam { ParamKey = "issue_code_prefix", ParamValue = "ISS", Description = "问题编号前缀" });
await db.SaveChangesAsync();
```

- [ ] **Step 4: 编译验证**

```bash
dotnet build ProjectManagement.Infrastructure/ProjectManagement.Infrastructure.csproj
```

---

### Phase 2: 后端 API — Controller 全部接口

---

### Task 5: 创建 `IssuesController`

**Files:**
- Create: `codes/backend/ProjectManagement.API/Controllers/IssuesController.cs`

**路由**: `/api/v1`

**需要注入的依赖**: `AppDbContext _db`, `IWebHostEnvironment _env`

---

- [ ] **Step 1: 项目内 — 问题列表 `GET /projects/{projectId}/issues`**

```csharp
[HttpGet("projects/{projectId:long}/issues")]
[RequirePermission("issue:list")]
public async Task<IActionResult> GetList(long projectId,
    [FromQuery] string? keyword, [FromQuery] string? issueType, [FromQuery] string? issueSource,
    [FromQuery] string? severity, [FromQuery] string? priority, [FromQuery] int? status,
    [FromQuery] long? assigneeId, [FromQuery] long? submitterId, [FromQuery] long? responsibleDeptId,
    [FromQuery] string? startDate, [FromQuery] string? endDate,
    [FromQuery] int pageIndex = 1, [FromQuery] int pageSize = 10)
{
    var (userId, _) = GetUserInfo();
    // 数据范围校验：能否访问该项目
    var codes = await ProjectPermissionHelper.GetUserPermissionCodesAsync(_db, userId);
    if (!await ProjectPermissionHelper.CanAccessProjectAsync(_db, projectId, userId, codes))
        return Forbid();

    var query = _db.ProjectIssues.Where(i => i.ProjectId == projectId);

    if (!string.IsNullOrWhiteSpace(keyword))
        query = query.Where(i => i.Title.Contains(keyword) || i.IssueCode.Contains(keyword));
    if (!string.IsNullOrWhiteSpace(issueType))
        query = query.Where(i => i.IssueType == issueType);
    if (!string.IsNullOrWhiteSpace(issueSource))
        query = query.Where(i => i.IssueSource == issueSource);
    if (!string.IsNullOrWhiteSpace(severity))
        query = query.Where(i => i.Severity == severity);
    if (!string.IsNullOrWhiteSpace(priority))
        query = query.Where(i => i.Priority == priority);
    if (status.HasValue)
        query = query.Where(i => i.Status == status.Value);
    if (assigneeId.HasValue)
        query = query.Where(i => i.AssigneeId == assigneeId.Value);
    if (submitterId.HasValue)
        query = query.Where(i => i.SubmitterId == submitterId.Value);
    if (responsibleDeptId.HasValue)
        query = query.Where(i => i.ResponsibleDeptId == responsibleDeptId.Value);
    if (DateOnly.TryParse(startDate, out var sd))
        query = query.Where(i => i.DiscoveredDate >= sd);
    if (DateOnly.TryParse(endDate, out var ed))
        query = query.Where(i => i.DiscoveredDate <= ed);

    var total = await query.CountAsync();
    var items = await query.OrderByDescending(i => i.CreatedAt)
        .Skip((pageIndex - 1) * pageSize).Take(pageSize)
        .ToListAsync();

    return Ok(new { code = 0, message = "success", data = new { items, total, pageIndex, pageSize } });
}
```

- [ ] **Step 2: 项目内 — 问题详情 `GET /projects/{projectId}/issues/{id}`**

```csharp
[HttpGet("projects/{projectId:long}/issues/{id:long}")]
[RequirePermission("issue:list")]
public async Task<IActionResult> GetDetail(long projectId, long id)
{
    var issue = await _db.ProjectIssues
        .Include(i => i.Measures.OrderBy(m => m.SortOrder))
        .FirstOrDefaultAsync(i => i.Id == id && i.ProjectId == projectId);
    if (issue == null) return Ok(new { code = 404, message = "问题不存在" });
    return Ok(new { code = 0, message = "success", data = issue });
}
```

- [ ] **Step 3: 项目内 — 创建问题 `POST /projects/{projectId}/issues`**

```csharp
public class CreateIssueRequest
{
    public string Title { get; set; } = "";
    public string? Description { get; set; }
    public string IssueSource { get; set; } = "";
    public string IssueType { get; set; } = "";
    public string Severity { get; set; } = "一般";
    public string Priority { get; set; } = "一般";
    public string? CauseAnalysis { get; set; }
    public DateOnly DiscoveredDate { get; set; }
    public DateOnly? PlannedDate { get; set; }
    public long? ResponsibleDeptId { get; set; }
    public string? ResponsibleDeptName { get; set; }
    public long AssigneeId { get; set; }
    public string? AssigneeName { get; set; }
    public long SubmitterId { get; set; }
    public string? SubmitterName { get; set; }
    public long? VerifierId { get; set; }
    public string? VerifierName { get; set; }
    public DateOnly? VerifiedDate { get; set; }
    public List<MeasureInput> Measures { get; set; } = new();
}

public class MeasureInput
{
    public int SortOrder { get; set; }
    public string Measure { get; set; } = "";
    public string? MeasureType { get; set; }
    public long? ResponsibleDeptId { get; set; }
    public string? ResponsibleDeptName { get; set; }
    public long? ResponsibleUserId { get; set; }
    public string? ResponsibleUserName { get; set; }
    public string? Remark { get; set; }
    public DateOnly? PlannedDate { get; set; }
}

[HttpPost("projects/{projectId:long}/issues")]
[RequirePermission("issue:create")]
public async Task<IActionResult> Create(long projectId, [FromBody] CreateIssueRequest req)
{
    var (userId, realName) = GetUserInfo();
    var project = await _db.Projects.FirstOrDefaultAsync(p => p.Id == projectId);
    if (project == null) return Ok(new { code = 404, message = "项目不存在" });

    // 生成问题编号：{项目编号}-ISS-{序号}
    var prefix = await _db.SysParams.Where(p => p.ParamKey == "issue_code_prefix")
        .Select(p => p.ParamValue).FirstOrDefaultAsync();
    if (string.IsNullOrWhiteSpace(prefix)) prefix = "ISS";
    var existingCount = await _db.ProjectIssues.CountAsync(i => i.ProjectId == projectId);
    var seq = (existingCount + 1).ToString().PadLeft(3, '0');
    var issueCode = $"{project.ProjectCode}-{prefix}-{seq}";

    var issue = new ProjectIssue
    {
        ProjectId = projectId,
        IssueCode = issueCode,
        Title = req.Title,
        Description = req.Description,
        IssueSource = req.IssueSource,
        IssueType = req.IssueType,
        Severity = req.Severity,
        Priority = req.Priority,
        CauseAnalysis = req.CauseAnalysis,
        DiscoveredDate = req.DiscoveredDate,
        PlannedDate = req.PlannedDate,
        ResponsibleDeptId = req.ResponsibleDeptId,
        ResponsibleDeptName = req.ResponsibleDeptName,
        AssigneeId = req.AssigneeId,
        AssigneeName = req.AssigneeName,
        SubmitterId = req.SubmitterId,
        SubmitterName = req.SubmitterName,
        CreatorId = userId,
        CreatorName = realName,
        VerifierId = req.VerifierId,
        VerifierName = req.VerifierName,
        VerifiedDate = req.VerifiedDate,
        Status = 0,
        ReopenCount = 0,
        CreatedAt = DateTime.UtcNow,
        Measures = req.Measures.Select((m, i) => new ProjectIssueMeasure
        {
            SortOrder = m.SortOrder > 0 ? m.SortOrder : (i + 1),
            Measure = m.Measure,
            MeasureType = m.MeasureType,
            ResponsibleDeptId = m.ResponsibleDeptId,
            ResponsibleDeptName = m.ResponsibleDeptName,
            ResponsibleUserId = m.ResponsibleUserId,
            ResponsibleUserName = m.ResponsibleUserName,
            Remark = m.Remark,
            PlannedDate = m.PlannedDate,
        }).ToList()
    };

    _db.ProjectIssues.Add(issue);
    await _db.SaveChangesAsync();
    return Ok(new { code = 0, message = "success", data = new { issue.Id, issue.IssueCode } });
}
```

- [ ] **Step 4: 项目内 — 编辑问题 `PUT /projects/{projectId}/issues/{id}`**

```csharp
[HttpPut("projects/{projectId:long}/issues/{id:long}")]
[RequirePermission("issue:edit")]
public async Task<IActionResult> Update(long projectId, long id, [FromBody] CreateIssueRequest req)
{
    var issue = await _db.ProjectIssues
        .Include(i => i.Measures)
        .FirstOrDefaultAsync(i => i.Id == id && i.ProjectId == projectId);
    if (issue == null) return Ok(new { code = 404, message = "问题不存在" });

    // 更新主表字段
    issue.Title = req.Title;
    issue.Description = req.Description;
    issue.IssueSource = req.IssueSource;
    issue.IssueType = req.IssueType;
    issue.Severity = req.Severity;
    issue.Priority = req.Priority;
    issue.CauseAnalysis = req.CauseAnalysis;
    issue.DiscoveredDate = req.DiscoveredDate;
    issue.PlannedDate = req.PlannedDate;
    issue.ResponsibleDeptId = req.ResponsibleDeptId;
    issue.ResponsibleDeptName = req.ResponsibleDeptName;
    issue.AssigneeId = req.AssigneeId;
    issue.AssigneeName = req.AssigneeName;
    issue.SubmitterId = req.SubmitterId;
    issue.SubmitterName = req.SubmitterName;
    issue.VerifierId = req.VerifierId;
    issue.VerifierName = req.VerifierName;
    issue.VerifiedDate = req.VerifiedDate;
    issue.UpdatedAt = DateTime.UtcNow;

    // 措施：全量替换
    _db.ProjectIssueMeasures.RemoveRange(issue.Measures);
    issue.Measures = req.Measures.Select((m, i) => new ProjectIssueMeasure
    {
        IssueId = id,
        SortOrder = m.SortOrder > 0 ? m.SortOrder : (i + 1),
        Measure = m.Measure,
        MeasureType = m.MeasureType,
        ResponsibleDeptId = m.ResponsibleDeptId,
        ResponsibleDeptName = m.ResponsibleDeptName,
        ResponsibleUserId = m.ResponsibleUserId,
        ResponsibleUserName = m.ResponsibleUserName,
        Remark = m.Remark,
        PlannedDate = m.PlannedDate,
    }).ToList();

    await _db.SaveChangesAsync();
    return Ok(new { code = 0, message = "success" });
}
```

- [ ] **Step 5: 项目内 — 删除问题 `DELETE /projects/{projectId}/issues/{id}`**

```csharp
[HttpDelete("projects/{projectId:long}/issues/{id:long}")]
[RequirePermission("issue:delete")]
public async Task<IActionResult> Delete(long projectId, long id)
{
    var issue = await _db.ProjectIssues
        .Include(i => i.Measures)
        .FirstOrDefaultAsync(i => i.Id == id && i.ProjectId == projectId);
    if (issue == null) return Ok(new { code = 404, message = "问题不存在" });

    _db.ProjectIssueMeasures.RemoveRange(issue.Measures);
    _db.ProjectIssues.Remove(issue);
    await _db.SaveChangesAsync();
    return Ok(new { code = 0, message = "success" });
}
```

- [ ] **Step 6: 项目内 — 状态变更 `PUT /projects/{projectId}/issues/{id}/status`**

```csharp
public class UpdateIssueStatusRequest
{
    public int Status { get; set; }
    public string? CauseAnalysis { get; set; }
    public long? VerifierId { get; set; }
    public string? VerifierName { get; set; }
    public DateOnly? VerifiedDate { get; set; }
}

[HttpPut("projects/{projectId:long}/issues/{id:long}/status")]
[RequirePermission("issue:edit")]
public async Task<IActionResult> UpdateStatus(long projectId, long id, [FromBody] UpdateIssueStatusRequest req)
{
    var issue = await _db.ProjectIssues.FirstOrDefaultAsync(i => i.Id == id && i.ProjectId == projectId);
    if (issue == null) return Ok(new { code = 404, message = "问题不存在" });

    // 状态校验
    if (req.Status == 1 && issue.Status == 0)
    {
        // 待处理 → 处理中
        issue.Status = 1;
    }
    else if (req.Status == 2 && issue.Status == 1)
    {
        // 处理中 → 已完成
        issue.Status = 2;
        if (req.VerifierId.HasValue) issue.VerifierId = req.VerifierId;
        if (!string.IsNullOrWhiteSpace(req.VerifierName)) issue.VerifierName = req.VerifierName;
        if (req.VerifiedDate.HasValue) issue.VerifiedDate = req.VerifiedDate;
    }
    else if (req.Status == 1 && issue.Status == 2)
    {
        // 已完成 → 处理中（重新打开）
        issue.Status = 1;
        issue.ReopenCount++;
    }
    else
    {
        return Ok(new { code = 400, message = "无效的状态变更" });
    }

    if (!string.IsNullOrWhiteSpace(req.CauseAnalysis))
        issue.CauseAnalysis = req.CauseAnalysis;
    issue.UpdatedAt = DateTime.UtcNow;

    await _db.SaveChangesAsync();
    return Ok(new { code = 0, message = "success" });
}
```

---

- [ ] **Step 7: 全局 — 我的问题列表 `GET /api/v1/issues/my`**

```csharp
[HttpGet("issues/my")]
[RequirePermission("issue:list")]
public async Task<IActionResult> GetMyIssues(
    [FromQuery] string? keyword, [FromQuery] string? issueType, [FromQuery] string? issueSource,
    [FromQuery] string? severity, [FromQuery] string? priority, [FromQuery] int? status,
    [FromQuery] long? projectId,
    [FromQuery] string? startDate, [FromQuery] string? endDate,
    [FromQuery] int pageIndex = 1, [FromQuery] int pageSize = 10)
{
    var (userId, _) = GetUserInfo();

    var query = _db.ProjectIssues.AsQueryable();

    // 数据范围：仅当前用户相关的问题
    query = query.Where(i => i.AssigneeId == userId
                           || i.SubmitterId == userId
                           || i.CreatorId == userId
                           || i.VerifierId == userId);

    // 叠加项目数据范围（复用现有 Project 行级过滤）
    var codes = await ProjectPermissionHelper.GetUserPermissionCodesAsync(_db, userId);
    if (!codes.Contains("admin"))
    {
        var scopeFilter = await ProjectDataScopeResolver.BuildListFilterAsync(_db, userId);
        if (scopeFilter != null)
        {
            var allowedProjectIds = await _db.Projects
                .FilterByScope(_db, scopeFilter)
                .Select(p => p.Id)
                .ToListAsync();
            query = query.Where(i => allowedProjectIds.Contains(i.ProjectId));
        }
    }

    // 常规筛选
    if (!string.IsNullOrWhiteSpace(keyword))
        query = query.Where(i => i.Title.Contains(keyword) || i.IssueCode.Contains(keyword));
    if (!string.IsNullOrWhiteSpace(issueType))
        query = query.Where(i => i.IssueType == issueType);
    if (!string.IsNullOrWhiteSpace(issueSource))
        query = query.Where(i => i.IssueSource == issueSource);
    if (!string.IsNullOrWhiteSpace(severity))
        query = query.Where(i => i.Severity == severity);
    if (!string.IsNullOrWhiteSpace(priority))
        query = query.Where(i => i.Priority == priority);
    if (status.HasValue)
        query = query.Where(i => i.Status == status.Value);
    if (projectId.HasValue)
        query = query.Where(i => i.ProjectId == projectId.Value);
    if (DateOnly.TryParse(startDate, out var sd))
        query = query.Where(i => i.DiscoveredDate >= sd);
    if (DateOnly.TryParse(endDate, out var ed))
        query = query.Where(i => i.DiscoveredDate <= ed);

    var total = await query.CountAsync();

    // JOIN 项目表带出 ProjectCode/ProjectName
    var items = await query.OrderByDescending(i => i.CreatedAt)
        .Skip((pageIndex - 1) * pageSize).Take(pageSize)
        .Join(_db.Projects, i => i.ProjectId, p => p.Id, (i, p) => new
        {
            i.Id, i.IssueCode, i.Title, i.IssueSource, i.IssueType, i.Severity, i.Priority,
            i.Status, i.DiscoveredDate, i.PlannedDate,
            i.AssigneeId, i.AssigneeName,
            i.SubmitterId, i.SubmitterName,
            i.CreatorId, i.CreatorName,
            i.ResponsibleDeptId, i.ResponsibleDeptName,
            i.CauseAnalysis, i.ReopenCount,
            i.VerifierId, i.VerifierName, i.VerifiedDate,
            i.CreatedAt, i.UpdatedAt,
            ProjectId = p.Id, ProjectCode = p.ProjectCode, ProjectName = p.ProjectName
        })
        .ToListAsync();

    return Ok(new { code = 0, message = "success", data = new { items, total, pageIndex, pageSize } });
}
```

- [ ] **Step 8: 全局 — 待处理问题计数 `GET /api/v1/issues/my/count`**

```csharp
[HttpGet("issues/my/count")]
public async Task<IActionResult> GetMyIssueCount()
{
    var (userId, _) = GetUserInfo();
    var count = await _db.ProjectIssues
        .Where(i => i.Status != 2)
        .Where(i => i.AssigneeId == userId
                 || i.SubmitterId == userId
                 || i.CreatorId == userId
                 || i.VerifierId == userId)
        .CountAsync();
    return Ok(new { code = 0, message = "success", data = new { count } });
}
```

- [ ] **Step 9: 编译验证**

```bash
dotnet build ProjectManagement.API/ProjectManagement.API.csproj
```

---

### Task 6: 项目删除级联问题

**Files:**
- Modify: `codes/backend/ProjectManagement.API/Controllers/ProjectController.cs` — `Delete` 方法

- [ ] **Step 1: 在 Delete 方法中增加级联删除**

在 `_db.Projects.Remove(project)` 之前：

```csharp
// 级联删除项目下的所有问题和措施
var issueIds = await _db.ProjectIssues.Where(i => i.ProjectId == id).Select(i => i.Id).ToListAsync();
if (issueIds.Count > 0)
{
    var measures = await _db.ProjectIssueMeasures.Where(m => issueIds.Contains(m.IssueId)).ToListAsync();
    _db.ProjectIssueMeasures.RemoveRange(measures);
    var issues = await _db.ProjectIssues.Where(i => i.ProjectId == id).ToListAsync();
    _db.ProjectIssues.RemoveRange(issues);
}
```

- [ ] **Step 2: 编译验证**

```bash
dotnet build ProjectManagement.API/ProjectManagement.API.csproj
```

---

### Phase 3: 前端 — 类型 + API 层

---

### Task 7: TypeScript 类型定义

**Files:**
- Create: `codes/frontend/src/types/issue.ts`

- [ ] **Step 1: 创建类型文件**

```typescript
export interface ProjectIssue {
  id: number
  projectId: number
  issueCode: string
  title: string
  description?: string
  issueSource: string
  issueType: string
  severity: string
  priority: string
  status: number          // 0=待处理 1=处理中 2=已完成
  causeAnalysis?: string
  discoveredDate: string
  plannedDate?: string
  responsibleDeptId?: number
  responsibleDeptName?: string
  assigneeId: number
  assigneeName?: string
  submitterId: number
  submitterName?: string
  creatorId: number
  creatorName?: string
  verifierId?: number
  verifierName?: string
  verifiedDate?: string
  reopenCount: number
  createdAt: string
  updatedAt?: string
  measures?: IssueMeasure[]
}

export interface IssueMeasure {
  id?: number
  issueId?: number
  sortOrder: number
  measure: string
  measureType?: string
  responsibleDeptId?: number
  responsibleDeptName?: string
  responsibleUserId?: number
  responsibleUserName?: string
  remark?: string
  plannedDate?: string
}

export interface IssueListResponse {
  items: ProjectIssue[]
  total: number
  pageIndex: number
  pageSize: number
}

// 全局视图额外带项目信息
export interface MyIssueItem extends ProjectIssue {
  projectCode: string
  projectName: string
}

export interface CreateIssueRequest {
  title: string
  description?: string
  issueSource: string
  issueType: string
  severity: string
  priority: string
  causeAnalysis?: string
  discoveredDate: string
  plannedDate?: string
  responsibleDeptId?: number
  responsibleDeptName?: string
  assigneeId: number
  assigneeName?: string
  submitterId: number
  submitterName?: string
  verifierId?: number
  verifierName?: string
  verifiedDate?: string
  measures: IssueMeasure[]
}

export interface UpdateIssueStatusRequest {
  status: number
  causeAnalysis?: string
  verifierId?: number
  verifierName?: string
  verifiedDate?: string
}

export const issueStatusMap: Record<number, string> = {
  0: '待处理',
  1: '处理中',
  2: '已完成'
}

export const issueStatusTagType: Record<number, string> = {
  0: 'warning',
  1: 'primary',
  2: 'success'
}
```

---

### Task 8: API 层

**Files:**
- Create: `codes/frontend/src/api/issue.ts`

- [ ] **Step 1: 创建 API 文件**

```typescript
import request from './request'
import type { IssueListResponse, ProjectIssue, CreateIssueRequest, UpdateIssueStatusRequest, MyIssueItem } from '@/types/issue'

// ── 项目内接口 ──
export function getProjectIssues(projectId: number, params?: Record<string, any>) {
  return request.get<IssueListResponse>(`/projects/${projectId}/issues`, { params })
}

export function getProjectIssue(projectId: number, id: number) {
  return request.get<ProjectIssue>(`/projects/${projectId}/issues/${id}`)
}

export function createProjectIssue(projectId: number, data: CreateIssueRequest) {
  return request.post(`/projects/${projectId}/issues`, data)
}

export function updateProjectIssue(projectId: number, id: number, data: CreateIssueRequest) {
  return request.put(`/projects/${projectId}/issues/${id}`, data)
}

export function deleteProjectIssue(projectId: number, id: number) {
  return request.delete(`/projects/${projectId}/issues/${id}`)
}

export function updateIssueStatus(projectId: number, id: number, data: UpdateIssueStatusRequest) {
  return request.put(`/projects/${projectId}/issues/${id}/status`, data)
}

// ── 全局接口 ──
export function getMyIssues(params?: Record<string, any>) {
  return request.get<{ items: MyIssueItem[]; total: number; pageIndex: number; pageSize: number }>('/issues/my', { params })
}

export function getMyIssueCount() {
  return request.get<{ count: number }>('/issues/my/count')
}

export function createIssue(data: CreateIssueRequest & { projectId: number }) {
  return request.post('/issues', data)
}
```

---

### Phase 4: 前端 — 组件开发

---

### Task 9: 创建/编辑弹窗 `IssueDialog.vue`

**Files:**
- Create: `codes/frontend/src/views/project/IssueDialog.vue`

**功能**：
- 三区域布局：基本信息 / 措施清单 / 附件
- 项目内模式：`projectId` 来自父组件 props
- 全局模式：表单顶部多一个「所属项目」下拉框（`showProjectSelect` prop）
- 责任部门 ↔ 责任人双向联动

- [ ] **Step 1: 创建组件骨架**

```vue
<script setup lang="ts">
import { reactive, ref, watch } from 'vue'
import { ElMessage } from 'element-plus'
import type { CreateIssueRequest, IssueMeasure, ProjectIssue } from '@/types/issue'

const props = defineProps<{
  visible: boolean
  issue?: ProjectIssue | null            // 编辑时传入
  projectId?: number                     // 项目内模式固定值
  showProjectSelect?: boolean            // 全局模式多一个项目选择
}>()

const emit = defineEmits<{
  (e: 'update:visible', v: boolean): void
  (e: 'saved'): void
}>()

// ── 表单数据 ──
const form = reactive<CreateIssueRequest & { projectId?: number }>({
  projectId: undefined,
  title: '',
  description: '',
  issueSource: '',
  issueType: '',
  severity: '一般',
  priority: '一般',
  causeAnalysis: '',
  discoveredDate: '',
  plannedDate: '',
  responsibleDeptId: undefined,
  responsibleDeptName: '',
  assigneeId: 0,
  assigneeName: '',
  submitterId: 0,
  submitterName: '',
  verifierId: undefined,
  verifierName: '',
  verifiedDate: '',
  measures: [{ sortOrder: 1, measure: '' }]
})

// ... 创建/保存逻辑（emit saved, ElMessage）
// ... 措施行增删排序
// ... 部门↔人员联动
// ... 字典下拉（issue_source, issue_type, measure_type）
</script>

<template>
  <el-dialog :model-value="visible" @update:model-value="$emit('update:visible', $event)"
    :title="issue ? '编辑问题' : '新建问题'" width="800px" destroy-on-close>
    <!-- 项目选择（全局模式） -->
    <!-- 区域一：基本信息 -->
    <!-- 区域二：措施清单 -->
    <!-- 区域三：附件 -->
    <template #footer>
      <el-button @click="$emit('update:visible', false)">取消</el-button>
      <el-button type="primary" @click="handleSave" :loading="saving">保存</el-button>
    </template>
  </el-dialog>
</template>
```

> 完整模板代码见实施时编写，需包含所有表单字段、动态措施表格、字典数据加载、部门/人员搜索联动。

- [ ] **Step 2: 验证** — TypeScript 不报错

---

### Task 10: 项目内问题 Tab `IssueTab.vue`

**Files:**
- Create: `codes/frontend/src/views/project/IssueTab.vue`

**功能**：
- 项目内问题列表（表格 + 筛选 + 分页）
- 行内状态操作按钮
- 点击编辑 → 弹出 `IssueDialog`
- 点击新建 → 弹出 `IssueDialog`（不传 `issue` prop）

- [ ] **Step 1: 组件接收 `projectId` prop**

```vue
<script setup lang="ts">
import { reactive, ref, onMounted } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import { getProjectIssues, deleteProjectIssue, updateIssueStatus } from '@/api/issue'
import { issueStatusMap, issueStatusTagType, type ProjectIssue } from '@/types/issue'
import IssueDialog from './IssueDialog.vue'

const props = defineProps<{ projectId: number }>()

const loading = ref(false)
const list = ref<ProjectIssue[]>([])
const total = ref(0)
const pagination = reactive({ page: 1, pageSize: 10 })
const filters = reactive({ keyword: '', issueType: '', issueSource: '', severity: '', priority: '', status: null as number | null })

const dialogVisible = ref(false)
const editing = ref<ProjectIssue | null>(null)

async function fetchData() { /* getProjectIssues(props.projectId, { ...filters, ...pagination }) */ }
function handleCreate() { editing.value = null; dialogVisible.value = true }
function handleEdit(row: ProjectIssue) { editing.value = row; dialogVisible.value = true }
async function handleDelete(row: ProjectIssue) { /* confirm → deleteProjectIssue → fetchData */ }
async function handleStatusChange(row: ProjectIssue, newStatus: number) { /* updateIssueStatus → fetchData */ }
</script>
```

> 表格使用 `el-table`，列：编号(`issueCode`)、标题(`title`)、类型(`issueType`)、严重程度(`severity`)、优先级(`priority`)、状态（Tag）、责任人(`assigneeName`)、责任部门(`responsibleDeptName`)、发现日期(`discoveredDate`)、计划完成(`plannedDate`)、重新打开次数(`reopenCount`)、操作。

- [ ] **Step 2: 验证** — 组件可独立渲染

---

### Task 11: 全局问题管理 `IssueManagement.vue`

**Files:**
- Create: `codes/frontend/src/views/project/IssueManagement.vue`

**功能**：参考 `TaskManagement.vue` 的结构，实现跨项目问题视图。

- [ ] **Step 1: 创建组件**

```vue
<script setup lang="ts">
import { reactive, ref, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage, ElMessageBox } from 'element-plus'
import { getMyIssues, updateIssueStatus } from '@/api/issue'
import { getProjectList } from '@/api/project'
import { issueStatusMap, issueStatusTagType, type MyIssueItem } from '@/types/issue'
import type { Project } from '@/types/project'
import IssueDialog from './IssueDialog.vue'

const router = useRouter()

const loading = ref(false)
const list = ref<MyIssueItem[]>([])
const total = ref(0)
const projects = ref<Project[]>([])
const pagination = reactive({ page: 1, pageSize: 20 })
const filters = reactive({
  keyword: '', issueType: '', issueSource: '', severity: '', priority: '',
  status: null as number | null, projectId: null as number | null
})

const dialogVisible = ref(false)

// 表格列：所属项目、编号、标题、类型、严重程度、优先级、状态、责任人、责任部门、发现日期、计划完成、重新打开次数、操作
// 「所属项目」列用 el-link 点击跳转 /project/view/{projectId}

async function fetchData() { /* getMyIssues({ ...filters, ...pagination }) */ }
function handleCreate() { dialogVisible.value = true }
// ...
</script>
```

- [ ] **Step 2: 验证** — 组件可独立渲染

---

### Task 12: ProjectEditor.vue 注册问题 Tab

**Files:**
- Modify: `codes/frontend/src/views/project/ProjectEditor.vue`

- [ ] **Step 1: 在项目详情 Tab 区添加「问题管理」Tab**

找到 Tab 定义区域（约在 `<el-tabs>` 中），在「文件资料」Tab 之前加入：

```vue
<el-tab-pane label="问题管理" name="issues" :lazy="true">
  <IssueTab v-if="activeTab === 'issues'" :project-id="projectId" />
</el-tab-pane>
```

- [ ] **Step 2: 导入组件**

```typescript
import IssueTab from './IssueTab.vue'
```

- [ ] **Step 3: 验证** — TypeScript 不报错

---

### Task 13: 路由 + 菜单 + Store 角标

**Files:**
- Modify: `codes/frontend/src/router/index.ts`
- Modify: `codes/frontend/src/config/menu.ts`
- Modify: `codes/frontend/src/store/auth.ts`

- [ ] **Step 1: 添加路由**

在 `codes/frontend/src/router/index.ts` 的 `project/tasks` 和 `project/files` 之间：

```typescript
{
  path: 'project/issues',
  name: 'IssueManagement',
  component: () => import('@/views/project/IssueManagement.vue')
},
```

- [ ] **Step 2: 添加菜单项**

在 `codes/frontend/src/config/menu.ts` 的「项目管理」children 中，`文件管理` 之前：

```typescript
{ path: '/project/issues', title: '问题管理', icon: 'Warning', roles: ['admin', 'templateAdmin'] },
```

- [ ] **Step 3: Store 角标计数**

在 `codes/frontend/src/store/auth.ts` 中：

```typescript
// 新增 state
const pendingIssueCount = ref(0)

// 新增 fetch 方法
async function fetchPendingIssueCount() {
  try {
    const res = await import('@/api/issue').then(m => m.getMyIssueCount())
    pendingIssueCount.value = res.data?.count ?? 0
  } catch { /* 静默失败 */ }
}
```

在侧边栏菜单渲染逻辑中，`问题管理` 菜单项使用 `pendingIssueCount` 显示角标（参考 `pendingCounts` 实现方式）。

- [ ] **Step 4: 编译前端**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/frontend"
npm run build
```

---

### Phase 5: 集成构建 + 验证

---

### Task 14: 全栈构建 + 验证

- [ ] **Step 1: 前端构建**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/frontend"
npm run build
```

- [ ] **Step 2: 后端构建**

```bash
cd "c:/Users/h/Desktop 2/Desktop/红黑榜/项目管理系统V1/codes/backend"
dotnet build ProjectManagement.API/ProjectManagement.API.csproj
```

- [ ] **Step 3: 运行验证清单**

对照 spec 第 9 节验收标准逐项验证（19 条）：

| # | 验证点 | 方法 |
|---|--------|------|
| A | 项目详情「问题管理」Tab 可见 | 打开任意项目详情页 |
| B | 新建问题 + 措施 | 点击新建 → 填写 → 保存 |
| C | 编号自动生成 | 检查 `{ProjectCode}-ISS-001` |
| D | 编辑问题 | 修改后保存 |
| E | 待处理→处理中 | 点击「开始处理」 |
| F | 处理中→已完成 | 点击「完成」+ 填验证人 |
| G | 重新打开 | 已完成后「重新打开」，ReopenCount +1 |
| H | 删除问题 | 确认删除 |
| I | 筛选 | 类型/来源/严重程度/优先级/状态 |
| J | 字典维护 | 系统管理→字典管理 |
| K | 权限校验 | 普通用户登录验证 |
| L | ReopenCount 展示 | 列表和详情 |
| M | 删除项目级联 | 删除项目后检查问题表 |
| N | 侧边栏菜单 | `/project/issues` 可访问 |
| O | 全局视图过滤 | 只显示自己相关的问题 |
| P | 所属项目列 | 点击跳转 |
| Q | 全局新建选项目 | 弹窗多一个下拉 |
| R | 角标 | 菜单旁数字 |
| S | 项目状态解耦 | 已完成/暂停项目也能建问题 |

---

## 4. 风险与应对

| 风险 | 应对 |
|------|------|
| 项目数据范围过滤与全局视图搭配复杂 | 全局视图后端分两步过滤：先按用户关联筛选 Issue，再 JOIN 项目表叠加 Project 行级权限 |
| 附件复用逻辑未对齐 `ProjectFileItem` 表结构 | 若 `ProjectFileItem` 无 `SourceType` 字段，需先新增字段（DDL 放入 DbInitializer 自愈） |
| 前端弹窗表单复杂度高 | `IssueDialog` 三区域分步加载，措施清单用独立子组件减少耦合 |

---

## 5. 工时估算

| Phase | 内容 | 估时 |
|-------|------|------|
| Phase 1 | 实体 + DbContext + Seed | 1h |
| Phase 2 | API Controller（9 个端点） | 2h |
| Phase 3 | 前端类型 + API 层 | 0.5h |
| Phase 4 | 前端组件（4 个 Vue 文件） | 3h |
| Phase 5 | 构建 + 验证 | 0.5h |
| **合计** | | **7h** |
